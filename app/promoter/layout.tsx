export const dynamic = "force-dynamic"

export default function PromoterLayout({
                                           children,
                                       }: {
    children: React.ReactNode
}) {
    return <>{children}</>
}